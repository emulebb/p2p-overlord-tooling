# ED2K Subsystem

Owns ED2K-specific shared helpers that are not tied to a single runtime.

Primary responsibilities:

- live server selection
- server rotation and round-robin materialization
- future ED2K-only normalization or evidence helpers
