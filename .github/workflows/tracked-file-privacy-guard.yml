name: Tracked File Guards

on:
  push:
    paths:
      - ".github/workflows/tracked-file-privacy-guard.yml"
      - "AGENTS.md"
      - "README.md"
      - "docs/**"
      - "cli/**"
      - "orchestration/**"
      - "subsystems/**"
      - "schemas/**"
      - "*.ps1"
      - "*.py"
  pull_request:
    paths:
      - ".github/workflows/tracked-file-privacy-guard.yml"
      - "AGENTS.md"
      - "README.md"
      - "docs/**"
      - "cli/**"
      - "orchestration/**"
      - "subsystems/**"
      - "schemas/**"
      - "*.ps1"
      - "*.py"

jobs:
  tracked-file-guards:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tracked-file privacy guard
        shell: pwsh
        run: .\overlord-tooling.ps1 guard-tracked-files

      - name: Run workspace conventions guard
        shell: pwsh
        run: .\overlord-tooling.ps1 guard-workspace-conventions
